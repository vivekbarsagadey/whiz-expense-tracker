/**
 * An optional enum of more granular transaction categories
 * (customize to fit your app's domain).
 */
export enum TransactionCategory {
    GROCERIES = 'GROCERIES',
    RENT = 'RENT',
    SALARY = 'SALARY',
    ENTERTAINMENT = 'ENTERTAINMENT',
    OTHER = 'OTHER',
  }